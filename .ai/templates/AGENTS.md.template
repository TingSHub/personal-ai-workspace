# {Project Name} — Project Instructions

<!-- 可选模板：仅在需要项目专属 Agent 指令时生成；默认使用 README 与顶层规则。 -->

遵循工作区顶层 AGENTS.md。项目入口、配置、依赖与复跑方式见本项目 README；执行契约以所引用的 Workflow SOP 为准。

## 项目专属指令

{仅填写本项目必需的补充指令；不复制顶层规则、README、阶段进度或完整资源表。无专属指令时无需创建本文件}
