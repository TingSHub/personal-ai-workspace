# 项目登记记录模板（V0.1.5）
# 字段清单一律从此模板读取填充，禁止内联字段定义
# 位置：.ai/projects/{project-name}.yaml（登记索引，元数据；物理项目在 projects/{project-name}/）
name: {project-name}
description: {一句话描述}
workflows:
  - {workflow by-name}
registered: {YYYY-MM-DD}
