# {Project Name}

{一句话说明项目为谁解决哪一类问题}

<!-- 创建时填写已知内容，未知项写明待验证；随实际执行补齐。此文件是导航，详细步骤和输入输出契约以 Workflow SOP 为唯一事实源。项目无需最终转成 Skill。 -->

## 目标与范围

{预期交付、适用场景与必要边界；探索期注明尚未验证的假设}

## 启动与输入输出

{入口 Workflow by-name、最小启动请求或命令及所需工作目录；简述用户需提供的输入和预期输出，详细契约引用对应 Workflow by-name。尚无 Workflow 时说明当前探索入口，不虚构可运行流程}

## 配置与依赖

{说明哪些业务输入、路径、账号或素材可替换，以及实际配置文件/模板的位置。记录必要运行环境和配置名称，不写凭据值；可复用脚本通过参数或配置接收变化项，明确尚存的本机/工作区依赖}

{Skill/Agent 按名称引用其资源记录，来源、installed_ref、调用与更新方法由 Resource Manager 维护，不在项目复制一份。其他工具、模板和素材注明可定位来源、实际使用位置及必要版本/使用限制；暂无依赖可写“无”}

## Workflows

{入口及子 Workflow by-name；说明入口关系，不复制 SOP。无已建 Workflow 时写明待形成}

## 最小复跑与验收

{主流程跑通后，引用一个可保留的小样例或输入构造方法、启动方式、预期结果与验收依据，使新会话无需原聊天即可复跑；优先复用现有测试/验收材料，不复制大产物或受限原始数据。创建时可写“待主流程验证”；不可复跑的环境限制如实说明}

## 维护约定

- 项目说明集中在 README，详细流程以 Workflow SOP 为准；沿用工作区顶层 Agent 规则，项目级 AGENTS.md / CLAUDE.md 按需添加。
- 实际交付优先；稳定执行方法维护在 Workflow SOP，单次输入、产物和日志与通用方法分开。复用经验的提升与回写遵循 Experience Curator，不因本约定跳过确认。
- 为可选 Skill 封装渐进保留上述材料，不预建 Skill 包、转换状态或通用框架，也不强制拆分子 Skill。
- 用户要求转换时，再由 skill-creator 基于现有入口、SOP、脚本、模板、依赖与复跑案例封装；先确定目标环境，处理实际需要的依赖/资源打包和路径适配，并验证封装后可独立完成同类任务。资源注册与安装交给 Resource Manager。
